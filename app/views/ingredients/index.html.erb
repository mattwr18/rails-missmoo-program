<p id="notice"><%= notice %></p>

<h1>Ingredients</h1>
<div class="table-responsive">
  <table class="table">
    <thead>
      <tr>
        <th>Name</th>
        <th>Amount</th>
        <th>Minimum amount</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @ingredients.each do |ingredient| %>
        <tr>
          <td><%= ingredient.name %></td>
          <td><%= ingredient.amount %> <%= ingredient.amount_type %></td>
          <td><%= ingredient.min_amount %> <%= ingredient.min_amount_type %></td>
          <td><%= link_to 'Show', ingredient %></td>
          <td><%= link_to 'Edit', edit_ingredient_path(ingredient) %></td>
          <td><%= link_to 'Destroy', ingredient, method: :delete, id: "delete_ingredient_#{ingredient.id}_from_index", data: { confirm: 'Are you sure?' } %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>

<br>

<%= link_to 'New ingredient', new_ingredient_path, class: "btn btn-lg btn-info" %>
